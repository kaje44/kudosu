<resources>

    <!-- Base application theme. -->
    <style name="KudosuTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/darkBackground</item>
        <item name="android:textAppearance">@style/normStyle</item>
        <item name="android:windowBackground">@color/appBackground</item>
    </style>

    <style name="KudosuTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="ToolBarStyle" parent="Theme.AppCompat">
        <item name="android:textColorPrimary">@color/colorText</item>
        <item name="android:textColorSecondary">@color/colorText</item>
        <item name="actionMenuTextColor">@color/colorText</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="fixStyle" parent="@android:style/TextAppearance">
        <item name="android:textSize">24dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:padding">20dp</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:textColor">@color/text</item>
    </style>

    <style name="normStyle" parent="@android:style/TextAppearance">
        <item name="android:padding">20dp</item>
        <item name="android:textSize">24dp</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:textColor">@color/text</item>
    </style>


    <style name="buttonNum" parent="@style/Widget.AppCompat.Button.Colored">
        <item name="android:layout_margin">2dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">24dp</item>
        <item name="android:textColor">@color/buttoText</item>
        <item name="android:background">@drawable/num_buttons</item>
    </style>

</resources>
